# Learning_Git
Learning Git Commands and Techniques.
Planning to create a simple classifier model to learn basics of Git followed by learning CI/CD on top of it.
The repo contains code for creating features and model which needs to be done via the pipeline file along with tests folder containing code for automatically testing if each script is executing as expected. Currently the code is blank and thus test cases will be added once finalized.